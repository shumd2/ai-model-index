## Overview

It's launch day. GPT-6 Sol positions itself as the reasoning workhorse of the GPT-6 line: same 1.05M-token window as Astra, $2/$10 pricing instead of $10/$50.

Two things stand out from the routing data already. Pro reasoning mode (the thing that used to cost extra on 5.6) is priced the same as standard Sol. And batch runs at $1/$5. The community called a GPT-6 Sol weeks ago; the prediction landed.


## Community insights

**Community section.** Real-world behaviour, pricing anecdotes, prompting
tips and failure modes live here. Nothing on this page is marketing copy;
send what you know and it gets added.
