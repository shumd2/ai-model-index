The GPT-6 family is now three deep. **GPT-6 Sol** positions below Astra at $2/$10 (batch $1/$5) with the same 1,050,000-token context window, and **GPT-6 Luna** replaces the most-routed model on the internet at $0.10/$0.50, half of 5.6 Luna's price.
Both ship with a Pro reasoning mode that costs the same as the standard tier, a break from the 5.6 generation where Pro commanded a premium. Both are already live on OpenRouter under the new `gpt-sol-latest` and `gpt-luna-latest` aliases, which now point at the 6 series.
The community rumor mill gets credit: chatter about a 'GPT-6 Sol' has been circulating for weeks. What nobody predicted was Luna's price cut.
